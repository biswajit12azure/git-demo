name: CICD Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD is done"
      - run: mkdir pipeline

  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI/CD is done"
