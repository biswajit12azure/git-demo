name: CI Pipeline

on: 
  push:
    branches: 
      - main
jobs:
   build: 
      runs-on: macos-latest
      steps:
        - name: Run Command
          run: echo "my name"

        - name: checkout code 
          uses: actions/checkout@v4
